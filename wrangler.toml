# Cloudflare Workers 服务名必须与控制台里的服务名一致（当前为 web2）
name = "web2"
main = ".output/server/index.mjs"
compatibility_date = "2025-07-15"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = ".output/public"
binding = "ASSETS"
